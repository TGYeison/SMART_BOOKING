export const CntMain = ({children}:{children: JSX.Element}) => (
    <div className="cnt__main cnt__main-static">
        {children}
    </div>
);

export const CntMainStatic = ({children}:{children: JSX.Element}) => (
    <div className="cnt__main cnt__main-static">
        {children}
    </div>
);

export const CntMainScroll = ({children}:{children: JSX.Element}) => (
    <div className="cnt__main cnt__main-scroll">
        {children}
    </div>
);

